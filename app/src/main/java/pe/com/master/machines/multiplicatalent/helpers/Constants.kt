package pe.com.master.machines.multiplicatalent.helpers

import pe.com.master.machines.multiplicatalent.BuildConfig

object Constants {

    object Extras {
        const val EXTRA_AUTOR = BuildConfig.APPLICATION_ID + ".extra.autor"
        const val EXTRA_TITLE = BuildConfig.APPLICATION_ID + ".extra.title"
    }
}